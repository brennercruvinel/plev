[package]
name = "lot"
version = "0.1.0"
edition = "2024"

[dependencies]
plev = { path = "../.." }
# the export half of the importer: lottie in, our .anm out (cnv.rs).
anm = { path = "../anm" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
