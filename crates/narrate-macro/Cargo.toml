[package]
name = "narrate-macro"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
description = "Proc-macro backend for the narrate UI DSL (experimental — not the official app pattern)"

[lib]
proc-macro = true

[dependencies]
syn.workspace = true
quote.workspace = true
proc-macro2.workspace = true

[dev-dependencies]
trybuild.workspace = true

[lints]
workspace = true
