# Build artifacts
target/
rust_out
tmp/
dist/
out/

# Android SDK/NDK (local toolchains, not part of the project)
ndk/
emulator/
platform-tools/
platforms/
build-tools/
system-images/
licenses/
.temp/

# Android build outputs & caches (never track)
android/.gradle/
android/app/build/
android/app/src/main/jniLibs/
android/build/
*.apk

# iOS build outputs & generated Xcode project + Info.plist (xcodegen regenerates them)
ios/showcase/build/
ios/showcase/ShowcaseDemo.xcodeproj/
ios/showcase/Info.plist

# Claude Code agent worktrees (keep only commands symlink)
.claude/worktrees/

# Claude Code memory (agent-local, not project artifacts)
memory/
MEMORY.md

# Dependencies
node_modules/
vendor/
.venv/
venv/

# Caches
.cache/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Coverage
coverage/
htmlcov/

# Experiments — source is versioned alongside other examples; build artifacts are not

# Private / experiments (not for public release)
examples-wip/
.claude/
# ref/: clones de estudo ficam fora; a curadoria (study-*.lua, READMEs de amostras) entra
ref/**
!ref/*/
ref/OpenUSD/
ref/winit/
!ref/*/study-*.lua
!ref/anim/swf-samples/
!ref/anim/swf-samples/README.md
!ref/lang/min-sized-rust.md

# Environment
.env
.env.local
.env.*.local
